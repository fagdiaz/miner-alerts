CODEX — Miner Alerts | Especificación de Trabajo Multi-Modelo (Constitución v1.5.0)

=============================================================================
ESTADO DEL PROYECTO: SPEC 039 Y SPEC 040 COMPLETADAS Y EN PRODUCCIÓN V3.2
=============================================================================
1. PRODUCCIÓN ACTIVA (V3.2.0 - PID 73268):
   - Servicio Windows `MinerAlerts` reiniciado y operando bajo PID 73268 (supervisado por NSSM PID 73280).
   - Consumo de recursos: ~59.8 MB RAM, 203 handles, ticks continuos cada 30s (`tick_sequence >= 3`).
   - Cero errores en `logs/err.log`.
   - Modos defensivos seguros activos por defecto (`dry_run: true`).

2. CARACTERÍSTICAS ACTIVAS Y COMANDOS TELEGRAM:
   - Spec 039 (Vnish Fan Governor):
     * Comandos: `/gov` (status), `/gov on`, `/gov off` (fallback seguro 100%), `/gov set <temp>`.
     * Supervisión de coolers: `/fans` (detecta y muestra tags `[MANUAL]` / `[AUTO]`).
   - Spec 040 (Dynamic Power & Preset Balancer para Elevadores Sensibles):
     * Comandos: `/balancer` (tabla ejecutiva agrupada por elevador), `/balancer on`, `/balancer off`,
       `/balancer setmax <miner> <preset>`, `/balancer run` (ciclo forzado) y `/balancer <id>`.
     * Motor determinista evaluando reinicios 24h/72h, cascadas grupales en elevadores y optimización de $H_{eff}$.

3. CERTIFICACIÓN GLOBAL DE PRUEBAS:
   - 575/575 tests PASS en 10.08s (0 fallos, 0 errores, 0 regresiones).
   - Suite de integración y concurrencia: `tests/test_preset_balancer_integration.py` (9/9 PASS).

=============================================================================
SIGUIENTES PASOS:
- Observación en caliente de logs de producción (`logs/out.log`) durante la operación regular.
- El operador puede consultar `/balancer` y `/gov` en Telegram para inspeccionar las sugerencias del sistema.
- Activar control en hardware real cuando el operador lo desee vía `/gov on` o `/balancer on`.
=============================================================================
MODELO SUGERIDO: Gemini 3.8 Flash High (Óptimo para soporte, consultas, análisis de métricas y siguiente especificación del roadmap)
=============================================================================