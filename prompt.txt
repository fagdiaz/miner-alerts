# ==============================================================================
# PROMPT DE EJECUCIÓN: PRÓXIMO PASO TRAS AUDITORÍA QA & SANEAMIENTO DE RESILIENCIA
# ==============================================================================
# MODELO RECOMENDADO: Gemini 3.8 Flash High (o Gemini 3.7 Flash High)
# Justificación: Implementación de Spec 069 (Telemetría Profunda por Cadena & Diagnóstico
# Predictivo Chain Break - PROP-008). Dominio puro de datos, modelos SQLite WAL,
# reglas estadísticas y tests exhaustivos sin alteraciones concurrentes críticas.
#
# LÍNEA BASE: 1181 tests PASS (0 fallos, 0 errores, 0 regresiones en 34.3s).
# SERVICIO WINDOWS: MinerAlerts RUNNING ininterrumpido.
# ==============================================================================

1. ESTADO ACTUAL DEL SISTEMA:
- Pruebas unitarias: 1181 tests PASS (0 fallos, 0 errores, 0 regresiones en 34.3s).
- Servicio Windows 'MinerAlerts': Running ininterrumpido.
- Rama: 'codex/022-adaptive-acquisition'
- Feature activa: 'specs/069-chain-telemetry-break-prediction'

2. TRABAJO COMPLETADO EN ESTA SESIÓN:
- Spec 070 (Fases C y D) COMPLETADA:
  * Extracción de DetectionHook y ActuatorHook en app/core/engine.py.
  * Desacoplamiento total de inspect.getsource(main) en los 4 tests legados.
  * 1160 tests PASS certificados.
- Spec 068 COMPLETADA & CERTIFICADA:
  * Servidor y cliente IPC nativo en app/ipc/watchdog_pipe.py vía Named Pipe (Windows NT) con SDDL 'D:(A;;GRGW;;;WD)'.
  * Fallback automático a TCP loopback socket 127.0.0.1:4029.
  * Helper de diagnóstico forense dump_thread_frames() con captura de trazas activas de hilos.
  * Integración en app/miner_monitor.py:main() en hilo daemon desacoplado con wake-up connect (<200ms shutdown).
  * Actualización de tools/monitor_watchdog.py con sondeo de alta frecuencia (<15s), máquina de 3 intentos, discriminador de deadlock supervisory y auto-recuperación de servicio.
- AUDITORÍA ESPECIALISTA QA & FIXES RESILIENTES:
  * Fix crítico para bug de kernel Windows NT ERROR_NO_DATA (232) mediante DisconnectNamedPipe() automático.
  * Salvaguarda de uptime contra falsos positivos de deadlock post-reinicio.
  * Blindaje defensivo con try/except logging en probe_ipc_and_recover().
  * Soporte de alias --config/-c en tools/audit_config.py.
  * 5 nuevas pruebas unitarias (total 1181 tests PASS).

3. PRÓXIMO OBJETIVO:
- Spec 069: Telemetría Profunda por Cadena & Diagnóstico Predictivo Chain Break (PROP-008).
  * Feature path: specs/069-chain-telemetry-break-prediction
  * Conexión directa al pool SQLite consolidado en Spec 071.
  * Índice compuesto 'ix_chain_telemetry_miner_chain_time'.
  * Regla de alerta preventiva de bus I2C (sensors_error_count > 0 por > 12h).
  * Discriminador de perturbación eléctrica vs fallo de silicio.

