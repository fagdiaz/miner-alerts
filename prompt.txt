CODEX — Miner Alerts | Especificación de Trabajo Multi-Modelo (Constitución v1.5.0)

=============================================================================
ESTADO DEL PROYECTO: CALIBRACIÓN TÉRMICA 83°C-84°C Y ANTI-SPAM EN PRODUCCIÓN
=============================================================================
1. PRODUCCIÓN ACTIVA (V3.2.0 - PID 3408):
   - Servicio Windows `MinerAlerts` reiniciado exitosamente y operando bajo PID 3408 (supervisado por NSSM PID 102080).
   - Ticks regulares autoritativos cada 30s (`tick_sequence >= 1`, `queue_depth: 0`).
   - Cero errores en ejecución.
   - Modos defensivos seguros activos por defecto (`dry_run: true`).

2. ESTRATEGIA OPERATIVA Y HARDENING APLICADO:
   - Estrategia Térmica:
     * Target del Gobernador de Ventiladores (Spec 039): 83.0°C (punto dulce de eficiencia y salud).
     * Deadband: [82.0°C, 83.5°C]. Si temp < 82°C reduce ventiladores (-2%); si > 83.5°C acelera (+3%).
     * Emergencia Ventiladores: 84.0°C -> Spike instantáneo a 100% PWM.
     * Desescalado Térmico en Balanceador (Spec 040): Si temp >= 84.0°C (headroom <= 1.0°C frente a corte 85°C)
       y ventiladores al 100%, baja secuencialmente el preset (`2700W -> 2500W -> 2300W`).
   - Mitigación de Spam en Telegram:
     * Saturación térmica: umbral elevado a 84.0°C con 98% PWM, racha de 5 ticks y cooldown de 7200s (2h).
     * Autotune Vnish: notificaciones desactivadas (`preset_alert_enabled: false`). Información disponible bajo demanda vía `/presets` y `/digest`.
   - Protección de Elevadores:
     * Desescalado grupal en cascada si mineros del mismo elevador reinician en < 30m.

3. CERTIFICACIÓN GLOBAL DE PRUEBAS:
   - 576/576 tests PASS en 8.66s (0 fallos, 0 errores, 0 regresiones).
   - Sintaxis limpia (`py_compile` exitoso en todos los módulos).
   - Commits registrados y sincronizados con `origin/codex/022-adaptive-acquisition`.

=============================================================================
SIGUIENTES PASOS:
- Monitoreo en caliente de la telemetría en `logs/out.log`.
- Los comandos `/balancer`, `/gov`, `/fans` y `/presets` en Telegram están listos para consulta.
- Si se desea que el sistema empiece a modular coolers o presets en hardware real, alternar con `/gov on` o `/balancer on`.
=============================================================================
MODELO SUGERIDO: Gemini 3.8 Flash High (Capacidad óptima para análisis, métricas operativas y siguientes tareas del roadmap)
=============================================================================