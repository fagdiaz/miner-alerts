# ==============================================================================
# PROMPT DE EJECUCIÓN: SPEC 071 COMPLETADA — PRÓXIMA: SPEC 072
# ==============================================================================
# MODELO RECOMENDADO: Gemini 3.8 Flash High
# Justificación: Spec 071 (P0/P1 SQLite Pool & Thread Hardening) está finalizada y
# validada con 1079 tests PASS (+7 tests, 0 fallos). El siguiente paso es Spec 072
# (P2 Unificación de Serialización de Estado & DRY Cleanups) que se encuentra dentro
# del alcance óptimo de Gemini Flash High.
# ==============================================================================

1. ESTADO ACTUAL Y LÍNEA BASE DEL SISTEMA:
- Pruebas unitarias: 1079 tests PASS (0 fallos, 0 errores, 0 regresiones en 32.7s).
- Servicio Windows 'MinerAlerts': Running ininterrumpido.
- Rama: 'codex/022-adaptive-acquisition'.
- Spec 071 Completada: Pool read-only SQLite centralizado (`open_readonly_connection` + `execute_readonly_with_retry` con backoff exponencial) migrado en los 7 módulos consumidores. Barreras defensivas de hilos daemon implementadas (`RestoreLock_{name}`, `ShutdownPurgeNotify`, `TelegramSender`, `TelegramPolling`).

2. ESPECIFICACIONES PAUSADAS Y SU HORIZONTE:
- Specs 068, 069, 070: Documentadas end-to-end (`specs/068-*/`, `069-*/`, `070-*/`), en pausa para ajustes post-limpieza arquitectónica.

3. PRÓXIMA ACCIÓN:
- Implementar Spec 072: Unificación de serialización de estado `MinerState` (`_build_state_payload` delegando en `StateManager._serialize_state()`), extracción del helper `find_assessment_by_target()` y centralización de `_dicts()`.
