CODEX — Miner Alerts | Especificación de Trabajo Multi-Modelo (Constitución v1.5.0)

=============================================================================
ESTADO DEL PROYECTO: ARQUITECTURA MODULAR DE DOMINIOS EN `app/` (SPEC 041 COMPLETA)
=============================================================================
1. PRODUCCIÓN ACTIVA & ARQUITECTURA MODULAR:
   - Servicio Windows `MinerAlerts` operando en vivo bajo NSSM (PID 71304):
     * Spec 039 Fan Governor: `fan_governor_enabled: true`, `fan_governor_dry_run: false`.
     * Spec 040 Preset Balancer: `preset_balancer_enabled: true`, `preset_balancer_dry_run: true`.
     * Spec 041 Arquitectura Modular: 22 archivos organizados en 4 subpaquetes de dominio desacoplados (`app/core/`, `app/vnish/`, `app/governance/`, `app/telegram/`) con 100% retrocompatibilidad vía shims con module aliasing.
   - Comandos Telegram Interactivos:
     * Gobernador: `/gov`, `/gov on`, `/gov off`, `/gov set <temp>`.
     * Balanceador: `/balancer`, `/balancer on`, `/balancer off`, `/balancer setmax <miner> <preset>`.
     * Elevadores: `/elevadores` (alias `/elevators`, `/sensibilidad`, `/elev`).
     * Monitoreo: `/status`, `/chart`, `/snooze`, `/digest`, `/fans`, `/efficiency`, `/presets`.

2. DOMINIOS IMPLEMENTADOS (`app/`):
   - `app/core/`: red API 4028, SQLite `EventStore`, episodios de alerta, evidencia fusionada, liveness, métricas Prometheus, calidad de minado y seguridad de reinicios (10 módulos).
   - `app/vnish/`: cliente REST autenticado, presets/autotuning, parser de logs y telemetría (4 módulos).
   - `app/governance/`: fan governor en lazo cerrado, preset balancer por elevador, salud térmica de fans y eficiencia energética J/TH (4 módulos).
   - `app/telegram/`: callbacks interactivos de alerta, gráficos visuales en RAM, digests matutinos, splitter y snooze (5 módulos).
   - Fachadas Shims en `app/`: garantizan `sys.modules[__name__] = _impl` para soporte transparente de imports y monkey-patching en tests y scripts existentes.
   - `app/miner_monitor.py`: modernizado para importar canónicamente desde los subpaquetes con fallback a shims.

3. CERTIFICACIÓN GLOBAL DE PRUEBAS & RELEASE:
   - 587/587 tests PASS en 11.26s (0 fallos, 0 errores, 0 regresiones).
   - Release Audit: PASS (`tools/release_audit.py --check-only`), 83 archivos de payload computados bajo SHA-256 `1d54510535bf6d089af3e84b75835da07b66643388e6d0ea4a2658372672f290`.
   - Cero quiebres en importaciones ni advertencias sintácticas (`py_compile` limpio).

=============================================================================
SIGUIENTES PASOS:
- Operación continua observando la respuesta térmica y eléctrica de los mineros 25 y 26 al escalar hacia los 2700W en el Elevador 2.
- Monitoreo de sensibilidad del Elevador 2 ante el aumento progresivo de carga (de ~4600W a 5400W).
- Mantener la higiene modular para cualquier nuevo desarrollo o refactorización futura.
=============================================================================
MODELO SUGERIDO: Gemini 3.8 Flash High (Capacidad óptima para análisis, métricas operativas y siguientes tareas del roadmap)
=============================================================================