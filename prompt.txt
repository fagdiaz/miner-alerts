# MINER ALERTS — SESIÓN DE CONTINUIDAD (SPEC 051 COMPLETADA, SIGUIENTE: COMMIT O SPEC 052)
# Motor recomendado: Gemini 3.8 Flash High (según protocolo de gobernanza AGENTS.md)

Hola agente. La **Spec 051 (Fast Phase Drop vs Connectivity Discriminator)** ha sido completamente implementada, integrada y certificada con **767/767 tests PASS** (0 fallos, 0 errores, 0 regresiones). El servicio Windows `MinerAlerts` se encuentra activo y estable.

---

## 1. ESTADO ACTUAL DEL REPOSITORIO Y COMPONENTES

- **Rama activa**: `codex/022-adaptive-acquisition`.
- **Producción**: Servicio `MinerAlerts` activo y supervisando la flota (PID 23652).
- **Suite global de pruebas**: **767/767 tests PASS** en 10.894s.
- **Spec 051 (Completada)**:
  * Dominio puro: `app/governance/phase_drop_discriminator.py` con `PhaseDropVerdict`, `PhaseDropAssessment`, `PhaseDropConfig`, `check_host_gateway_reachability`, `evaluate_phase_drop`, `process_phase_drop_cycle` y `render_phase_drop_alert`.
  * Mobile-First UX: 100% de las tarjetas de alerta cumplen estrictamente con `visible_line_width <= 32`.
  * Integración en monitor: `app/miner_monitor.py` evalúa caídas concurrentes post-adquisición, suprime la histeresis de 3 ticks ante cortes de circuito, encola alertas Telegram prioritarias y persiste eventos `electrical_phase_drop` en `event_store`.
  * Pruebas unitarias: `tests/test_phase_drop_discriminator.py` (12 tests PASS).
  * Pruebas de integración: `tests/test_phase_drop_integration.py` (6 tests PASS).
  * Documentación: `specs/051-phase-drop-discriminator/tasks.md` (6/6 completadas), `evidence.md`, `docs/audit/DEVELOPMENT_LOG.md` y `docs/speckit/ROADMAP.md` actualizados.
- **Spec 052 (Especificada de Punta a Punta, Pendiente de Implementación)**:
  * `specs/052-maintenance-scheduler/`: `spec.md`, `checklists/requirements.md`, `plan.md`, `tasks.md`, `evidence.md`.
  * Planificador de ventanas de mantenimiento eléctrico programado (`/schedule_maintenance`) con rampa de descarga a T-10m y parada a T-0.

---

## 2. SIGUIENTES ACCIONES DISPONIBLES

1. **Opción A (Commit y Push Spec 051)**:
   - Crear commit feature-scoped para Spec 051 y hacer push a `origin/codex/022-adaptive-acquisition`.
2. **Opción B (Implementar Spec 052: Maintenance Scheduler)**:
   - Actualizar `.specify/feature.json` a `specs/052-maintenance-scheduler`.
   - Implementar Fase 1 (`app/governance/maintenance_scheduler.py` y `tests/test_maintenance_scheduler.py`).
   - Implementar Fase 2 (comandos Telegram `/schedule_maintenance`, `/scheduled`, `/cancel_maintenance` e integración en el monitor).
   - Validar suite de pruebas.

