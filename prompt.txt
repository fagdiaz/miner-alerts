GEMINI 3.8 FLASH HIGH — Miner Alerts | Spec 045 Completado -> Spec 046: Fleet Diagnostic Card Layouts

ROL Y MOTOR RECOMENDADO
Motor: Gemini 3.8 Flash High
Rol: Diseñador e implementador de Spec 046 (armonización y rediseño Mobile-First de reportes de diagnóstico de flota: /status, /fans, /efficiency, /presets).

ESTADO ACTUAL CERTIFICADO (Spec 045)
- Spec 045 (Telegram Mobile Help Center) COMPLETADA, integrada y certificada:
  * Módulo puro `app/telegram/help_center.py` con 28 comandos canónicos y 5 categorías temáticas.
  * Parser `parse_help_callback` con límite estricto de <= 64 bytes UTF-8.
  * Tarjetas y vistas táctiles respetando <= 32 caracteres visibles por línea (Condición C1).
  * Conexión bidireccional Command Center (`/menu`) <-> Help Center (`/help`) vía botón en fila 4 (`help:nav:home`).
  * Router de callbacks en `_handle_callback_query` con ACK temprano (<50ms `answer_callback_query`) y edición in-place.
  * Soporte enriquecido para `/info <cmd>` y registro de `/menu` y `/silent` en whitelists.
  * Fallback seguro en `_send_telegram_direct` preservando `reply_markup` (Condición C6).
  * Suite completa del repositorio: 660/660 tests PASS en 10.56s (0 fallos, 0 regresiones).
  * Release audit: PASS (digest `4c1d77311f67049c1ece885108210e2e8626e7bd05833d5360ec0bb06bf41851`, 62 payload files).

ARCHIVOS CLAVE DEL SISTEMA
1. `docs/speckit/RFC_TELEGRAM_MOBILE_UX_OPTIMIZATION.md` (especialmente Sección 5.B, 5.C, 5.D y condiciones C1-C10).
2. `app/telegram/help_center.py` (patrón canónico de formato <= 32 cols, `visible_line_width`, `wrap_mobile_lines`).
3. `app/telegram/command_center.py` (patrón de inline keyboards y navegación táctil).
4. `app/governance/fan_health.py` (formato actual de `/fans` a migrar a tarjetas móviles).
5. `app/governance/energy_efficiency.py` (formato actual de `/efficiency` a migrar).
6. `app/vnish/presets.py` / `app/governance/preset_balancer.py` (formato actual de `/presets` a migrar).
7. `app/miner_monitor.py` (renderizador de `/status` de flota a encapsular y formatear en tarjetas verticales).

SIGUIENTE PASO: SPEC 046
1. Crear `specs/046-mobile-card-layouts/` con `spec.md`, `plan.md`, `tasks.md`, `evidence.md`.
2. Actualizar `.specify/feature.json` a `specs/046-mobile-card-layouts`.
3. Migrar las salidas diagnósticas de flota a tarjetas verticales acotadas a 32 caracteres visibles:
   - `/status`: Snapshot de flota con viñetas `•`, potencia, temperatura máxima y botones `[ 🔄 Actualizar ] [ 📊 Gráfico ] [ 📱 Menú ]`.
   - `/fans`: Tarjetas verticales por minero (Temp, Fans RPM/PWM, Modo Governor).
   - `/efficiency`: Tarjetas verticales (J/TH, TH/s, Watts).
   - `/presets`: Perfil actual, target, margen térmico y estado del balancer.
4. Mantener total desacoplamiento de I/O, pureza en los renderizadores y suites de prueba unitarias.