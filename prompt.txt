CODEX — Miner Alerts | Especificación de Trabajo Multi-Modelo (Constitución v1.4.0)

=============================================================================
POLÍTICA DE MODELOS Y DELEGACIÓN ESCALADA (CONSTITUCIÓN v1.4.0)
=============================================================================
- MOTOR PRINCIPAL: Gemini 3.7 Flash High para lectura, análisis, lógica pura, schemas, suites completas de tests (unitarios, deterministas, integración, benchmarks) y trazabilidad.
- ESCALAMIENTO ON-DEMAND: Claude Sonnet 4.6 (Thinking) reservado para concurrencia viva multi-hilo en producción (`miner_monitor.py`), sockets API 4028 y refactor de máquina de estados/reboot.
- ESCALAMIENTO EXTREMO: Claude Opus 4.6 (Thinking) strictly para deadlocks cíclicos o rediseños mayores de arquitectura.

=============================================================================
ESTADO ACTUAL PRE-CARGADO (NO REPETIR RELEVAMIENTO)
=============================================================================
- Spec 021 (Liveness Watchdog): COMPLETADO Y CERRADO al 100% tras 77.3h de soak continuo sin fallos.
- Spec 022 (Adaptive Acquisition): T001-T012 COMPLETADOS (371 tests PASS).
  - T009: Aislamiento read-only de diagnósticos verificado.
  - T011: Invariantes pre-rollout verificados (0 imports de acción, Telegram o startup guard).
  - T012: Comparación shadow, paridad determinista y ensayo de rollback dinámico (3 tests PASS).
- Spec 023 (Incident Evidence Fusion): Código 100% completado (T001-T018). Queda solo observación runtime tras Spec 022.
- Repositorio Git: Rama `codex/022-adaptive-acquisition`.
- Suite global: 371 tests ejecutados en 2.7s sin regresiones ni fallos (0 fallos, 0 errores, 0 skips).

COMANDO DE VERIFICACIÓN DE PRUEBAS (371 tests actuales):
.\.venv\Scripts\python.exe -c "import unittest, os; loader = unittest.TestLoader(); suite = unittest.TestSuite(); [suite.addTests(loader.discover('tests', pattern=f)) for f in os.listdir('tests') if f.startswith('test_') and f.endswith('.py')]; runner = unittest.TextTestRunner(verbosity=0); res = runner.run(suite); print(f'PASS: {res.testsRun} tests, failures={len(res.failures)}, errors={len(res.errors)}')"

DIRECTIVAS DE LECTURA DE CONTEXTO RÁPIDO:
1. `specs/022-adaptive-acquisition/tasks.md` (T013 pendiente: Activación controlada y observación D+1/D+3).
2. `specs/022-adaptive-acquisition/evidence.md` (Historial completo de validación hasta T012).
3. `AGENTS.md` y `docs/speckit/DELIVERY_PLAN.md` (Reglas de rollout en producción).

=============================================================================
OBJETIVO DE ESTA ITERACIÓN: SPEC 022 — T013 (Activación Controlada en Producción)
=============================================================================

1. Procedimiento de Activación Controlada (T013):
   - Al haberse cumplido y cerrado el soak D+3 de Spec 021, la producción está habilitada para recibir la adquisición adaptativa.
   - Planificar la ventana de activación:
     * Alinear `app/config.json` con `"adaptive_acquisition_enabled": true`.
     * Ejecutar el reinicio ordenado del servicio Windows `MinerAlerts`.
     * Verificar que el nuevo PID adquiera el mutex, inicie con el startup guard intacto (600s), y emita los logs de adquisición adaptativa (`ADAPTIVE_ACQUISITION enabled=true workers=2`).
     * Verificar en `monitor_heartbeat.json` que los ticks progresen normalmente con queue_depth=0.

2. Actualización de Trazabilidad:
   - Registrar la evidencia del nuevo PID, hora de inicio y primeros ticks en `specs/022-adaptive-acquisition/evidence.md`.
   - Marcar `[x] T013` (inicio de soak D+1/D+3) y preparar el cierre de Spec 022 (T014).

=============================================================================
MODELO RECOMENDADO: Claude Sonnet 4.6 (Thinking) (por manejo de servicio en producción)
=============================================================================
=============================================================================
ESTADO: T015 COMPLETADO ✓

- `_KNOWN_TABLES` → "incident_assessments" agregado.
- `_latest_assessments(connection, *, since_ts, limit)` → consulta bounded read-only,
  filtrada por `assessment_now_ts >= since_ts`, newest-first, sin scoring ni inferencia.
- `build_dashboard_data` → llama `_latest_assessments`, expone `incident_assessments`
  en el dict y `summary["assessments"]` como contador.
- `_render_assessment_rows(assessments)` → renderer puro de filas almacenadas
  (subject_ref, subject_type, status, ruleset_version, digest[:16]), sin re-scoring.
- `render_dashboard_html` → nueva sección "Evaluaciones de incidente / Lectura / sin
  accion automatica" usando `_render_assessment_rows`.
- 10 nuevos tests en `tests/test_operations_dashboard.py` (todos PASS).
- Suite completa: 323/323 PASS (0 fallos, 0 errores). py_compile dashboard: OK.
- FR-011 verificado: `operations_dashboard.py` no contiene ninguna llamada a funciones
  de scoring de evidence_fusion (confirmed by source-text assertion test).

=============================================================================
NOTA DE CIERRE T009+T011 — 2026-08-27 (Claude Sonnet 4.6 Thinking)
=============================================================================
ESTADO: T009 COMPLETADO ✓ | T011 COMPLETADO ✓

T009 — Diagnostic Read-Only Isolation:
- `DiagnosticProbeResult` y `EpisodeDiagnosticEnvelope`: frozen dataclasses verificados.
- `collect_diagnostic` → siempre Authority.DIAGNOSTIC, filtrado por dispatch_authoritative.
- Sin poll_health.record_epoch, sin stats, sin campos de acción.
- diagnostics_enabled=False cuando adaptive_acquisition_enabled=False (doble-flag guard).

T011 — Invariantes pre-rollout:
- py_compile app/miner_monitor.py + app/acquisition.py → SYNTAX OK.
- Estado: dispatch_authoritative filtra DIAGNOSTIC y LATE determinísticamente.
- Acción: acquisition.py sin hashcore, subprocess, miner_states, streak, send_telegram.
- Telegram: acquisition.py sin bot_token, chat_id, getUpdates, update_id.
- Startup guard: AcquisitionConfig sin campos startup_guard/grace_period.
- Presupuesto: workers [1-4], diagnostic = 1 summary + 0 stats.

24 nuevos tests en `tests/test_t009_t011_invariants.py`, todos PASS.
Suite completa: 368/368 PASS (0 fallos, 0 errores).

PRÓXIMA TAREA: Spec 022 T012 — Shadow comparison (24h de métricas, parity check,
rehearsal de rollback). Leer `specs/022-adaptive-acquisition/tasks.md`.