CODEX — Miner Alerts | Especificación de Trabajo Multi-Modelo (Constitución v1.5.0)

=============================================================================
ESTADO DEL PROYECTO: PURGA LIMPIA DE SHIMS Y TESTS MODERNIZADOS (SPEC 042 COMPLETA)
=============================================================================
1. PRODUCCIÓN ACTIVA & MÁXIMA PULCRITUD EN `app/`:
   - En la raíz de `app/` solo residen:
     * `app/miner_monitor.py` (orquestador raíz de producción).
     * `app/__init__.py` (inicializador formal del paquete).
     * Archivos locales de runtime (`config.json`, `config.example.json`, `state.json`).
     * 4 subpaquetes de dominio desacoplados: `app/core/`, `app/vnish/`, `app/governance/`, `app/telegram/`.
   - Cero shims planos sueltos en `app/`: los 22 archivos puente fueron eliminados con éxito.
   - Servicio Windows `MinerAlerts` operando en vivo bajo NSSM (PID 71304):
     * Spec 039 Fan Governor: `fan_governor_enabled: true`, `fan_governor_dry_run: false`.
     * Spec 040 Preset Balancer: `preset_balancer_enabled: true`, `preset_balancer_dry_run: false`.
   - Comandos Telegram Interactivos:
     * Gobernador: `/gov`, `/gov on`, `/gov off`, `/gov set <temp>`.
     * Balanceador: `/balancer`, `/balancer on`, `/balancer off`, `/balancer setmax <miner> <preset>`.
     * Elevadores: `/elevadores` (alias `/elevators`, `/sensibilidad`, `/elev`).
     * Monitoreo: `/status`, `/chart`, `/snooze`, `/digest`, `/fans`, `/efficiency`, `/presets`.

2. MODERNIZACIÓN CANÓNICA DE TESTS (`tests/`):
   - Todos los 44 archivos de test en `tests/` fueron modernizados para importar canónicamente desde `app.core`, `app.vnish`, `app.governance` y `app.telegram`.
   - Todos los `@patch(...)` de unittest mock fueron actualizados a sus rutas canónicas reales.
   - 587/587 tests PASS en 10.68s (0 fallos, 0 errores, 0 regresiones).

3. CERTIFICACIÓN GLOBAL DE PRUEBAS & RELEASE:
   - Compilación sintáctica: 100% OK (`py_compile app\miner_monitor.py`).
   - Release Audit: PASS (`tools/release_audit.py --check-only`), 60 archivos de payload limpios bajo SHA-256 `b11a92084d2f558327290d98c05b7fa691e512ddc7f33d36e7f0e120459249fd`.
   - Watchdog de producción: 100% `healthy=true suppressed=false reasons=none`.

=============================================================================
SIGUIENTES PASOS:
- Operación continua observando la respuesta térmica y eléctrica de los mineros 25 y 26 al escalar hacia los 2700W en el Elevador 2.
- Monitoreo de sensibilidad del Elevador 2 ante el aumento progresivo de carga (de ~4600W a 5400W).
- Mantener la higiene modular para cualquier nuevo desarrollo o refactorización futura.
=============================================================================
MODELO SUGERIDO: Gemini 3.8 Flash High (Capacidad óptima para análisis, métricas operativas y siguientes tareas del roadmap)
=============================================================================